# apiVersion: eksctl.io/v1alpha5
# kind: ClusterConfig

# metadata:
#   name: roboshop
#   region: us-east-1

# managedNodeGroups:
#   - name: spot
#     instanceType: m5.large
#     desiredCapacity: 3
#     spot: true


apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: learn-eks
  region: eu-west-2

managedNodeGroups:
  - name: ondemand
    instanceType: m7i-flex.large
    desiredCapacity: 1
    minSize: 1
    maxSize: 4
